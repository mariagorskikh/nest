# SPDX-License-Identifier: Apache-2.0
# Manifest-bound capability-token scenario: coordinator -> 3 intermediaries ->
# 12 leaves, then four adversarial probes for manifest tampering, scope
# escalation, stale ancestors, and audience confusion.
#
# Under `auth: manifest_delegatable` the manifest_delegated_auth validators PASS. Switch this to
# `jwt` to see all four validators FAIL against the central re-issuance
# baseline.
name: manifest_delegated_auth
description: "Manifest-bound delegatable auth with cascading revocation and audience binding."

tier: 1
seed: 42

agents:
  count: 17
  brain: state-machine
  roles:
    - name: coordinator
      count: 1
    - name: intermediary
      count: 3
    - name: leaf
      count: 12
    - name: auditor
      count: 1

layers:
  transport: in_memory
  comms: nest_native
  identity: did_key
  registry: in_memory
  auth: manifest_delegatable
  trust: score_average
  payments: prepaid_credits
  coordination: contract_net
  negotiation: alternating_offers
  memory: blackboard
  privacy: noop
  datafacts: datafacts_v1

task:
  type: manifest_delegated_auth
  config:
    intermediaries: 3
    leaves_per_intermediary: 4

failures:
  message_drop: 0.0

duration: "ticks: 10000"

metrics:
  - success_rate
  - message_count
  - agent_count

output:
  trace: ./traces/manifest_delegated_auth.jsonl
